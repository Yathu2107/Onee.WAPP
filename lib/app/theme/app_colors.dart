import 'package:flutter/material.dart';

/// Onee brand palette — use only these colors.
class AppColors {
  AppColors._();

  static const Color cream = Color(0xFFFBECB3);
  static const Color nearBlack = Color(0xFF060605);
  static const Color gold = Color(0xFFEBB407);
  static const Color mutedBrown = Color(0xFF847454);
  static const Color white = Color(0xFFFFFFFF);
}
